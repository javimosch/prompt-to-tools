<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Interaction</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">
    <div class="container mx-auto p-4 bg-white shadow-md rounded-md">
        <h1 class="text-2xl font-bold mb-4">Interact with LLM API</h1>


        <form action="/pretty-output" method="post" class="mb-4">
            <div class="mb-4">
                <label for="prettyPrompt" class="block text-gray-700 text-sm font-bold mb-2">Enter your prompt for pretty output:</label>
                <input type="text" id="prettyPrompt" name="prompt" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" placeholder="e.g., How to fetch drivers">
            </div>
            <button type="submit" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
                Get Pretty Output
            </button>
        </form>

        <% if (rawOutput) { %>
            <div class="mt-8 p-4 border rounded bg-gray-50">
                <h2 class="text-lg font-bold mb-2">Raw API Output:</h2>
                <pre class="whitespace-pre-wrap"><%= rawOutput %></pre>
            </div>
        <% } %>

        <% if (prettyOutput) { %>
            <div class="mt-8 p-4 border rounded bg-green-50">
                <h2 class="text-lg font-bold mb-2">Pretty Output:</h2>
                <div >
                                    <%- prettyOutput %>
                </div>
            </div>
        <% } %>

        <% if (error) { %>
            <p class="text-red-500 mt-4"><%= error %></p>
        <% } %>
    </div>
</body>
</html>
                </div>
            </div>
        <% } %>

        <% if (error) { %>
            <p class="text-red-500 mt-4"><%= error %></p>
        <% } %>
    </div>
</body>
</html>
